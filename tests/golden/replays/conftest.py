"""Per-subsystem fixtures for golden-replay tests (currently empty).

The `--update-golden` flag and the `update_golden` fixture are registered in
`tests/conftest.py` so they're available to non-golden tests as well.
"""
